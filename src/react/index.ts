/**
 * `@taaltreelabs/on-device-llm/react`
 *
 * React hooks: `useAvailability`, `useChat`, `useGenerate`
 * (docs/plan.md §2, §5 Phase 4). React-only; requires `react` (and, for
 * the app-facing hooks, `react-native`) as peer dependencies.
 *
 * Nothing here yet — this is a Phase 0 scaffold placeholder.
 */

/** Marks this module as present; replaced by real exports in Phase 4. */
export const REACT_PLACEHOLDER = true;

/** Placeholder for the future `useChat` status union. */
export type ChatStatus = 'idle';
